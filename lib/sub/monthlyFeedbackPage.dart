import 'package:flutter/material.dart';

class MonthlyFeedbackApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('네번째 페이지'),
        ),
      ),
    );
  }
}
