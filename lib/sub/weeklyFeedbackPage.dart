import 'package:flutter/material.dart';

class WeeklyFeedbackApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('세번째 페이지'),
        ),
      ),
    );
  }
}
